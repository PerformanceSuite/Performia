{
  "UserPromptSubmit": {
    "command": "./.claude/cleanup.sh",
    "description": "Automatic repository cleanup and organization",
    "matchers": ["/end-session", "/exit", "/quit", "exit", "quit"]
  }
}
