# Core infrastructure values
GCP_PROJECT=
REGION=us-central1
GCS_BUCKET=
PUBSUB_PREFIX=musicai
ARTIFACT_REPO=song-map
CLOUD_RUN_IMAGE_TAG=dev-local

# Service settings
INGEST_MAX_RUNTIME_SEC=900
SEPARATION_STEMS_DIR=gs://$GCS_BUCKET/stems
PACKAGER_OUTPUT_DIR=gs://$GCS_BUCKET/final

# Optional integrations
DB_URL=postgresql://user:pass@host:5432/dbname
BIGQUERY_DATASET=
